<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"

      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<targets>
		<target name="logConsole" xsi:type="Console"
				layout="${date}|${level:uppercase=true}|${message}
				${exception:format=tostring}|${logger}|${all-event-properties}"/>

		<target name="infoLogFile" xsi:type="File" fileName="InfoLogFile.txt"
				layout="${longdate} * ${level} * ${message}" keepFileOpen="false"/>

		<target name="errorLogFile" xsi:type="File" fileName="ErrorLogFile.txt"
				layout="${longdate} * ${level} * ${message}" keepFileOpen="false"/>
		
		<target name="debugLogFile" xsi:type="File" fileName="DebugLogFile.txt"
				layout="${longdate} * ${level} * ${message}" keepFileOpen="false"/>
	</targets>

	<rules>
		<logger name="*" minlevel="Trace" writeTo="logConsole"/>
		<logger name="*" minlevel="Info" maxlevel="Info" writeTo="infoLogFile"/>
		<logger name="*" minlevel="Error" maxlevel="Fatal" writeTo="errorLogFile"/>
		<logger name="*" minlevel="Debug" maxlevel="Debug" writeTo="debugLogFile"/>
		<logger name="*" minlevel="Error" maxlevel="Error" writeTo="debugLogFile"/>
	</rules>
	
</nlog>